# TrueHire — Verified Job Portal
# Requirements for all 4 files:
#   truehireweb.py        (Streamlit web app)
#   data_preprocessing.py (Stage 1 — clean raw CSV)
#   tfidf_features.py     (Stage 2 — TF-IDF vectorizer)
#   train_model.py        (Stage 3 — PAC fraud-detection model)
#
# Install: pip install -r requirements.txt
# Run    : streamlit run truehireweb.py

streamlit>=1.35.0
pandas>=2.0.0
numpy>=1.26.0
scikit-learn>=1.3.0
scipy>=1.11.0
joblib>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
